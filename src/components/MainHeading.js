import React from 'react';

const MainHeading = (props) => {
    return (
        <div className="main-heading">
            <h1>{props.heading}</h1>
        </div>
    )
}

export default MainHeading;